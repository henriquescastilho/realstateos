# api-node

Backend Node.js + Express em migração.
Este serviço ainda não substitui `apps/api` (FastAPI).
Skeleton ativo — apenas rota `/health` implementada.

## Rodar local

```bash
npm install && npm run dev
```
